Import db fpsi.sql path === FPSI>public>fpsi.sql
Change db password FPSI>.env 
Change DB_PASSWORD to none

To Run: 
open cmd
Redirect to laravel folder === cd "C:\Users\........\FPSI"
Type command === php artisan serve 
